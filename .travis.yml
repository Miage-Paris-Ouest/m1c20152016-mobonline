language: android
android:
  components:
    - platform-tools
    - build-tools-23.0.1
    - android-23
before_install:
 - chmod +x ./StudyAtNanterre/gradlew
 - export ANDROID_HOME=$PWD/android-sdk-linux
 - export PATH=${PATH}:${ANDROID_HOME}/tools:${ANDROID_HOME}/platform-tools
install: sh gradle assemble
script: gradle check
   #- ./StudyAtNanterre/gradlew build